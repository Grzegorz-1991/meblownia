<h1 class = "text-center mt-4">Kategoria <%= @category.name %></h1>

<% if logged_in? && current_user.admin? %>
    <div class="text-center mt-4">
        <%= link_to "Edytuj nazwę kategorii", edit_category_path(@category), class: "btn btn-primary" %>
    </div>
<% end %>
</p>
<h3 class = "text-center mt-4">Meble</h3>

<div class="flickr_pagination">
    <%= will_paginate @mebles, :container => false %>
</div>
<%= render 'mebles/mebles'%>

<div class="flickr_pagination mb-4">
    <%= will_paginate @mebles, :container => false %>
</div>